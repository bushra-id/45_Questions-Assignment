
// if you haven't recived a array index error in one of your programs yet,try to make one happen.

// creating array
let newArray:string[] = ['apple','banana','cherry'];

console.log(newArray[3]);//this will throw an error because the maximum index

// make sure you correct the error before closing the program

// correct the error
console.log(newArray);